launch:
  - node:
      pkg: "sub_hardware"
      exec: "gpio_node.py" 
      name: "gpio_node" 
      output: "screen"
      param:
        - from: "$(find-pkg-share sub_hardware)/config/params.yaml"
