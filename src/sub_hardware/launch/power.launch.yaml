launch:
  - node:
      pkg: "sub_hardware"
      exec: "actuator_node.py" 
      name: "actuator_node" 
      output: "screen"
      param:
        - from: "$(find-pkg-share sub_hardware)/config/params.yaml"
