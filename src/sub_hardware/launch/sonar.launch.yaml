launch:
  - node:
      pkg: "sub_hardware"
      exec: "sonar_node.py" 
      name: "sonar_node" 
      output: "screen"
      param:
        - from: "$(find-pkg-share sub_hardware)/config/params.yaml"
