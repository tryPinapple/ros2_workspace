launch:
  - node:
      pkg: "sub_hardware"
      exec: "indicator_node.py" 
      name: "indicator_node" 
      output: "screen"
      param:
        - from: "$(find-pkg-share sub_hardware)/config/params.yaml"
